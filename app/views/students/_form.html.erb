<%= simple_form_for(@student) do |f| %>
	<%= f.error_notification %>
	<div class="form-group">
		<%= f.input :name, :label => "Nome", :input_html => {:class => "form-control"} %>
	</div>

	<div class="form-group">
		<%= f.input :register_number, :label => "Número de Registro", :input_html => {:class => "form-control"} %>
	</div>

	<div class="form-group">
		<%= f.input :status, collection: [[:"Ativo (a)", 1],[:"Inativo (a)", 2]], :input_html => {:class => "form-control"} %>
	</div>

	<div class="form-actions">
		<%= f.button :submit, :value => "Salvar", :class => "btn btn-info" %>
	</div>
<% end %>

